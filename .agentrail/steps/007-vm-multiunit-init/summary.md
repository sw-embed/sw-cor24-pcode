Added .p24m multi-unit image loading to pvm.s. _start detects P24M magic and parses header to set up code, globals, IRT, and unit table. Extended vm_state to 42 bytes (unit_table_ptr, p24m_base). Extended .p24m format (27-byte header with code_off/globals_off, 9-byte unit table entries with per-unit irt_off). Fixed critical op_xcall bug where return_pc was corrupted by incorrect COR24 stack ordering. End-to-end verified: app xcalls mathlib.double(21)=42 across two units. Backward compat preserved. pvmasm.s vm_state synced.