Update the P-code VM (pvm.s and pvmasm.s) to support loading and running .p24m multi-unit images. Extend vm_state from 27 to 39 bytes adding: unit_table(3), irt_base(3), unit_count(1), current_unit(1), gp_base(3), padding(1). Update _start initialization to detect .p24m magic and parse the header: read unit table, IRT, set unit_count, current_unit=0, code=combined code base, gp_base from unit_table[0]. Update op_xcall to use the real IRT (irt_base + per-unit offsets). Update op_ret to detect cross-unit returns by checking static_link high byte -- if nonzero, restore current_unit and gp_base. Update LOADG/STOREG to use gp_base instead of gp. Backward compatible: v1 .p24 files still work (unit_count=1, current_unit=0). See docs/load-time-resolution.md section 7. Test: end-to-end two-unit program loaded as .p24m runs correctly with cross-unit calls and returns.