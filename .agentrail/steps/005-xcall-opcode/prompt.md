Add the XCALL opcode (0x74) to the P-code VM and both assemblers. In pa24r: add XCall=0x74 to the opcode enum with IMM16 encoding (opcode + 2-byte LE slot index), assemble 'xcall <symbol>' by looking up the symbol in the import/extern list and emitting 0x74 + slot. In pasm.s: mirror the same. In pvm.s: add op_xcall handler that reads a 16-bit slot index, looks up the absolute target address from IRT[current_unit][slot], builds a call frame (reusing static_link high byte for caller's unit_id), sets pc=target, updates current_unit and gp_base. Extend dispatch table from 116 to at least 117 entries. In pvmasm.s: mirror pvm.s changes. See docs/load-time-resolution.md sections 3 (xcall instruction), 4 (assembler), and 7 (VM XCALL handler). Test: single-unit program with a manually-constructed IRT, verify xcall dispatches correctly.