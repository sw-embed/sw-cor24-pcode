Implemented MEMCPY (0x70) and MEMSET (0x71) opcodes across all five implementations (pvm.s, pvmasm.s, pasm.s, pa24r, pv24t). MEMCPY uses memmove semantics with direction-dependent copy (backward when src < dst). Both use temp-var loops due to COR24 3-register constraint. Added 3 test programs (basic copy, fill, overlapping). Fixed pre-existing clippy/fmt issues in tracer. All 152 Rust + 15 VM tests pass.