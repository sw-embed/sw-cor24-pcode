Extended .p24 binary format to v2 with export/import tables. Added XCall opcode (0x74) with Imm16 encoding to pa24r, pasm.s, pvmasm.s, and tracer. New assembler directives: .unit, .import, .export, .extern with slot allocation. V2 header includes export table (hash+offset), import table (unit_hash+name_hash+slot), string table, unit name. FNV-1a 16-bit hash for name matching. 10 new integration tests. Backward compatible with v1. Added design doc docs/load-time-resolution.md and saga steps 004-010.