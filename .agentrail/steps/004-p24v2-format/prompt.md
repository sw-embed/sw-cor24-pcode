Extend the .p24 binary format to v2 with export and import tables. Update pa24r (assembler/src/lib.rs) to: (1) add version 0x02 support, (2) when .unit + .export directives are present, collect exports and write an export table to the .p24 header, (3) add .import directive recognition and import slot allocation for .extern symbols, (4) emit import table in the .p24. Mirror all changes in pasm.s. Backward compatible: v1 files still work unchanged. See docs/load-time-resolution.md section 2 (.P24 Binary Format v2) and section 4 (Assembler Changes). Test: assemble a unit with exports, verify the .p24 contains the export table.