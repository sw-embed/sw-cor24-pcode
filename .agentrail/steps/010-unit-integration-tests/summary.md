End-to-end multi-unit integration tests: mathlib exports gcd+factorial, app xcalls both, output '6720' matches static version. Added code relocation to p24-load (patches jmp/jz/jnz/call/calln operands with code_base for non-zero units, plus data-ref push relocation). Expanded pvm.s stacks from 96 to 768 bytes for recursive programs. demo.sh check_multiunit() runs full pipeline. 3 new .spc test files, 20 VM tests total.