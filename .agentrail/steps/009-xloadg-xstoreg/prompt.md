Add XLOADG (0x75) and XSTOREG (0x76) opcodes for cross-unit global access. Encoding: opcode(1) + unit_id(1) + offset(1) = 3 bytes. In pvm.s/pvmasm.s: add handlers that compute address as unit_table[unit_id].global_base + offset*3, then load/store the eval stack TOS. In pa24r: add opcodes to enum, IMM8+IMM8 encoding. In pasm.s: mirror. Extend dispatch table to 119 entries. See docs/load-time-resolution.md section 7 (Cross-Unit Globals). Test: two-unit program where one unit reads/writes the other's globals via xloadg/xstoreg.