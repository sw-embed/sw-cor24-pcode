Add --unit flag to pl24r (linker). In unit mode: (1) merge input .spc modules as before, (2) PRESERVE .unit/.import/.export/.extern directives in output (do not strip them), (3) validate that every .extern symbol is either defined internally (resolve as regular call) or declared via .import (leave as .extern for assembler), (4) validate every .export symbol is defined, (5) error on missing exports or unresolvable externs without .import. The existing static-link mode (no --unit flag) remains unchanged. See docs/load-time-resolution.md section 5. Test: multi-module unit linked with --unit, assembled into a v2 .p24 with correct export/import tables.