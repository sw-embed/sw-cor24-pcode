Added --unit flag to pl24r. In unit mode: preserves .unit/.import/.export/.extern in output, validates exports are defined, resolves internal externs (removes them) or requires .import for external ones. Parser handles .unit/.import/.endunit. link_unit()/emit_unit() public API. End-to-end test: unit-linked .spc assembles to v2 .p24 with correct export table. 6 new tests, all 188 pass.