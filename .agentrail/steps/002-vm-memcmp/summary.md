Implemented MEMCMP (0x72) as VM opcode across all 5 toolchain components: pvm.s, pvmasm.s, pasm.s, pa24r, pv24t. Stack effect (a b len -- result) with lexicographic byte comparison returning -1/0/1. Updated dispatch tables to 115 entries, mnemonic tables, opcode enums. Added t14-memcmp test covering equal, less-than, greater-than, and zero-length cases. Fixed COR24 ISA constraints (no sw z or sub z operands, ceq register ordering). All 152 Rust + 16 VM tests pass.